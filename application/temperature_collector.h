void soilCollector2(MQTTClient* client) {
    // String umidadeSolo = String(analogRead(PIN_SENSOR_UMIDADE_SOLO));
    // int myInt = atoi(umidadeSolo.c_str());
    // int myHumidity = myInt;
    // // int myHumidity = (4095 - myInt);
    // char myChar[10];
    // sprintf(myChar, "%d", myHumidity);
    // String myString = "123.00";
    // String httpRequestData = "{\"humidity\":" + myString + ",\"sensor_id\": \""+SENSOR_ID+"\",\"temperature\": \"0.00\"}";
    // Serial.println(httpRequestData);

    // sendJsonToAWS(client, httpRequestData, AWS_IOT_PUBLISH_TOPIC_TEMPERATURE);
}